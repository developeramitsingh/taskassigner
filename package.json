{
  "name": "amit-taskassigner",
  "version": "1.0.0",
  "description": "a task assigner app",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild":"NPM_CONFIG_PRODUCTION=false npm install --prefix taskassigner && npm run build --prefix taskassigner"
  },
  "author": "amit singh",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1"
  }
}
